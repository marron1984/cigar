# Cigar Intelligence Platform — 既存コード解析と差分実装計画

要件定義書「Cigar Cafe 次期機能」を、いまのコードベースに照らして精査した結果。
**結論：要件の約4割はすでに実装済み。本当のギャップは「認証」「銘柄単位のマスタ」
「構造化された味覚データ」「集合知（公開レビュー・平均評価・ランキング）」の4つ。**

---

## 1. 既存コードベースの解析（要件書 §89 の10項目）

| # | 項目 | 現状 |
|---|---|---|
| 1 | Framework | **なし（素のJS）**。ビルドはデータ生成のみ（tools/*.js）。Next.js / TypeScript / Prisma は不使用 |
| 2 | 構成 | `index.html` 1枚のシェル＋ビュー切替SPA。`js/*.js` はIIFEモジュール（計約6,900行）。`data/*.js` がデータ層。`en/` は生成される英語シェル |
| 3 | Database | Supabase Postgres。テーブル2つ：`cigar_notes`（id / created / owner / **data jsonb**）と `cigar_shares`。**主保存先はブラウザ**（IndexedDB＋localStorage）で、クラウドは任意の同期先 |
| 4 | 認証 | **なし**。記録者は自由入力の名前文字列。RLSは anon キーに全件読み書きを許可（信頼できる少人数前提） |
| 5 | 既存の記録ノート | 充実。銘柄名・ブランド・産地・サイズ・強さ・日付・価格（外貨換算つき）・喫煙時間（タイマー）・場所・メモ・写真（複数、1400pxに縮小）・**0.5刻み10段階評価**・AI講評・共有（画像カード/リンク/テキスト＋ハッシュタグ） |
| 6 | User Model | なし（owner文字列のみ） |
| 7 | 葉巻データ | **ブランド単位**のマスタ254件（14カ国、`data/brands.js` 2.1MB）。ビトラは各ブランドの自由文字列配列で、**銘柄（Robustos等）単位のIDは無い** |
| 8 | Components | コンポーネントシステムなし。テンプレートリテラル＋共通CSS。UI文字列は `T()`（i18n辞書946行） |
| 9 | Design System | `css/style.css` のCSS変数（--panel / --gold 等）で統一。ダーク・和文セリフ基調 |
| 10 | Deploy | Vercel静的配信（buildCommand無効化済み）。GitHub Actionsでニュース毎日更新。AIはSupabase Edge Function |

### 要件書の技術想定とのずれ

要件書の推奨（Next.js / TypeScript / Prisma / Server Actions）と現実（静的サイト＋Supabase）は
大きく異なる。ただし要件書自身が **§67「既存スタック優先」§89「既存設計を壊さず実装」** と
明記しているため、本計画は **静的フロント＋Supabase（Auth / Postgres / Storage / Edge Functions）
のまま拡張する**。理由：

- 既存30ページのSEO資産（パスルーティング・構造化データ・OG・sitemap）を作り直すリスクが大きい
- Phase 1〜2 のサーバ要件（認証・集計・ランキング）は Supabase の Auth / RLS / SQLビューで足りる
- 運用費ゼロの静的配信を維持できる
- 「30秒で記録」は既存ノートUIがすでに達成している（必須入力は銘柄名1つ）。置換は退行になる

Phase 3（協調フィルタリング等）で本格的なサーバが要る時が来たら、その時点でデータモデルを
そのまま持って Next.js 等へ段階移行できる。**だからこそ今回はデータモデル設計を最優先にする。**

---

## 2. 要件との差分表

### すでに有るもの（実装不要 or 小修正）

| 要件 | 現状 | 差分 |
|---|---|---|
| §7 喫煙記録 | 記録ノート | 体感強さ・購入日ほか数項目の追加のみ |
| §8 0.5刻み評価 | 星の左右タップで半個刻み | **完了済み** |
| §70-73 30秒記録・段階的開示 | 必須は銘柄名のみ、詳細は`<details>`で展開 | **完了済み**（思想も一致） |
| §16 写真 | 複数枚・自動縮小 | 保存先の変更のみ（後述） |
| §19 吸いたいリスト | ブランド大全★→記録ノート連携 | ログイン時のクラウド同期を追加 |
| §40-42 統計 | 月別チャート・産地上位・評価分布・サイズ別喫煙時間・年間まとめ | レーダーチャート等の追加のみ |
| §61-64 SEO/OGP | パスURL・JSON-LD・OG画像・sitemap | 銘柄ページ分の拡張のみ |
| §82-83 多言語 | 日英二言語が完全稼働 | 新機能のi18n追従 |
| §65 SNS共有 | 画像/リンク/テキスト＋ハッシュタグ自動生成 | **完了済み**（要件を超過） |
| §78 初回ユーザー | はじめの一本さがし（アンケート式） | Cold Start の受け皿として流用 |
| 番外 | ヒュミドール在庫・AI講評・写真AI自動入力・用語クイズ | 要件に無い既存資産 |

### 無いもの（本当のギャップ）

| 要件 | ギャップ | 依存 |
|---|---|---|
| §66 認証 | ユーザー概念そのものが無い。**全ての公開系機能の前提** | — |
| §5 葉巻マスタ | 銘柄（cigar）単位のID・slug・寸法・葉構成が無い | — |
| §9-14 味覚データ | フレーバータグ・ドロー・燃焼・ボディ・体感強さが無い（自由記述メモのみ） | マスタ不要 |
| §17 銘柄詳細ページ | /cigars/{brand}/{slug} が無い | マスタ |
| §19,34 公開レビュー・平均評価・ランキング | 共有DBは「少人数の合同ノート」であり公開集合知ではない | 認証＋マスタ |
| §22-31 Taste DNA・レコメンド | finderは履歴を使わない | 味覚データ |
| §45-46 KPI・イベントログ | 無い | 認証（無くても匿名で可） |
| §47-50 Admin CRUD | 現Adminは閲覧＋削除のみ。マスタはgit管理 | — |

### 設計上の重要な不一致（先に決めるべきこと）

1. **正準値の言語**（§82）：要件は「DBは英語、表示は日本語」。現状は**逆**（DBに「キューバ」、
   表示で英訳）。→ 新設テーブルは英語正準＋slug。既存記録は取り込み時に一括正規化
   （`I18N` の対訳表がそのまま変換表として使える）。
2. **公開/非公開**（§7）：現状はクラウド同期＝共有DBに入る＝実質全員が読める。記録単位の
   visibility が無い。→ Phase 0 のRLS再設計で `private / public` を導入。**既定は private**（現状踏襲）。
3. **写真の保存先**：現状は base64 を jsonb に格納（1枚数百KB。管理画面タイムアウトの原因にも
   なった）。→ 公開記録の写真は Supabase Storage へ移し、URLだけをDBに持つ。
4. **マスタ強制はしない**：§81「自由入力よりMaster選択」は、§98「記録が面倒なサービスにしない」
   と衝突し得る。→ 銘柄はマスタから選べるが**自由入力も常に許可**（cigar_id は nullable、
   名前一致で後から紐付け）。30秒ルールを優先する。

---

## 3. データモデル（Supabase SQL）

要件書 §51 の13テーブルを、既存資産に合わせて9テーブル＋2ビューに圧縮する。

```
profiles          auth.users 1:1（username, avatar_url, bio, country）
brands            data/brands.js から自動生成（254行。ja/en/slug/country/founded…）
cigars            新設マスタ。brand_id, name, slug, vitola, length_mm, ring_gauge,
                  strength, wrapper, binder, filler, price_jpy, status…
                  （初期データは各ブランドの代表ヴィトラから編纂。数百件から育てる）
smoking_logs      記録の正式テーブル。既存 cigar_notes.data を列に展開して移行。
                  cigar_id は nullable ＋ name自由入力を併存。
                  追加列: draw_rating, burn_rating, body_rating, strength_felt,
                  purchase_date, aging_days(生成列), visibility('private'|'public')
flavors           36タグ（§9のリスト。category, slug, ja, en）
smoking_log_flavors  log_id × flavor_id（intensity は列だけ用意しUIは後回し＝§10通り）
favorites         user_id × cigar_id
wishlists         user_id × cigar_id（既存localStorageの吸いたいリストをログイン時に取り込み）
event_logs        追記専用（§46の10イベント）
--- ビュー ---
cigar_stats       銘柄別の平均評価・件数・評価分布・フレーバー上位（public記録のみ集計）
cigar_ranking     ベイズ補正 WR = v/(v+m)·R + m/(v+m)·C（m=5, C=全体平均）※§34そのまま
```

- **reviews テーブルは作らない**。公開された smoking_log がそのままレビュー（Vivino方式、§19と同じ結果）
- **user_preferences も作らない**（Phase 2 では自分の記録からクライアント側で算出できる。
  協調フィルタリングを入れる Phase 3 で materialized view として導入）
- RLS：smoking_logs は `owner = auth.uid()` で読み書き、`visibility='public'` は誰でも読める。
  現在の「anonキーで全部読み書き」ポリシーは**廃止**

### 既存データの移行

- `cigar_notes`（jsonb）→ `smoking_logs`（列）への一括移行SQLを用意。産地・サイズは英語正準へ変換
- 記録者名→アカウントの紐付けは、少人数なので「初回ログイン時に自分の名前を選んで引き取る」フロー
- 移行後も `cigar_shares`（共有リンク）は互換のため残す
- ブラウザのIndexedDBは**オフラインキャッシュとして存続**（ローカルファーストは維持）

---

## 4. 実装フェーズ

### Phase 0 — 基盤（他の全てが依存）
1. Supabase Auth 導入（メールMagic Link＋Google。§66）
2. profiles テーブル＋RLS全面再設計＋既存記録の引き取りフロー
3. 写真を Storage へ（公開記録のみ。私的記録は現行どおりローカル）
4. event_logs＋クライアント `track()`（§46。ファーストパーティのみ）

### Phase 1 — 記録の構造化と銘柄ページ（§84）
5. brands / cigars マスタ生成ツール（`tools/export_master_sql.js`）＋初期シード
6. 記録フォーム拡張：銘柄ピッカー（マスタ候補＋自由入力）、詳細開示の中に
   フレーバーチップ（7分類36種）・ドロー・燃焼・ボディ・体感強さ・購入日・公開/非公開
   ※既定は全て任意。必須は今までどおり銘柄名だけ
7. 銘柄詳細ページ `/cigars/{brand}/{slug}/`：静的シェルを build パイプラインで生成
   （既存 build_pages.js の拡張）＋ cigar_stats ビューをクライアントから読んで
   平均評価・評価分布・フレーバーチャート・公開レビューを表示。
   「吸った」→記録フォームへプリフィル（既存 prefillNew を流用）
8. Favorite（♥）を Wishlist（★）と分離。両方クラウド同期
9. SEO：銘柄ページの Product / AggregateRating / BreadcrumbList JSON-LD（レビューが付いた銘柄のみ）

### Phase 2 — 分析と発見（§85）
10. Taste DNA：自分の記録から産地/強さ/サイズ/フレーバーの嗜好プロファイルを算出
    （クライアント側で完結。§79のCold Start分岐込み：0件→人気、1-2件→類似、10件以上→DNA）
11. ルールベース・レコメンド（§28の重み付けスコアをそのまま実装：Flavor35/Strength20/
    Country15/Vitola10/Wrapper10/Price10。未喫煙のマスタ銘柄から抽出）
12. 似ている葉巻（§30。マスタ属性のコサイン類似でビルド時に事前計算）
13. ランキングページ（§33の8カテゴリ。cigar_ranking ビュー＋1時間キャッシュ）
14. 検索強化：産地×価格×強さ×サイズ×フレーバー×評価の複合絞り込み（§36）
15. Discover ページ（§39）

### Phase 3 — 将来（§86。今回はテーブル設計のみ担保）
協調フィルタリング／バッジ／パスポート／フォロー・フィード／AI推薦

### 実装しないもの（§87どおり）
EC・決済・配送・オークション・ユーザー間売買・DM・ライブ配信

### 法務（§88）
公開レビュー機能の解禁と同時に、20歳以上確認（初回モーダル）と免責表示を footer に追加。

---

## 5. 受け入れ基準の現在地（§92）

| 基準 | 現在 | 到達フェーズ |
|---|---|---|
| 葉巻を検索できる | △ ブランドまで | P1 |
| 詳細ページを閲覧できる | ✗ | P1 |
| 「吸った」と記録できる | ✔ | 済 |
| 0.5刻みで評価 | ✔ | 済 |
| Flavor複数選択 / Draw / Burn | ✗ | P1 |
| 価格・写真・メモ・編集・削除 | ✔ | 済 |
| 公開・非公開を選べる | ✗（実質全公開の合同ノート） | P0 |
| Favorite / Wishlist | △ Wishlistのみ | P1 |
| 過去の記録を確認 | ✔ | 済 |
| 平均評価・公開レビュー | ✗ | P1 |

## 6. 最初の一手

Phase 0-1 は独立に検証可能な7つのPRに分割できる。着手順：

1. **RLS再設計＋Auth**（これだけで公開範囲の歪みが直る。既存UIへの影響は最小）
2. **マスタSQL生成ツール**（brands 254件は機械変換。cigars は代表ヴィトラから初期500件目標）
3. **記録フォームの構造化項目**（マスタが無くても動く設計なので並行可能）

North Star（§94-95「月間記録数」）に効くのは 3 が最初。1-2 はその土台。
